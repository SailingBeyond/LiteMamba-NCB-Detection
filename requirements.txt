# Python >= 3.10
#
# Install PyTorch separately for the target CPU/CUDA environment before
# installing these packages. The repository uses torch, but it is intentionally
# not pinned here because the correct wheel depends on the local hardware and
# CUDA setup.

numpy>=1.23
pandas>=1.5
matplotlib>=3.6
scikit-learn>=1.2
h5py>=3.8
tqdm>=4.64
umap-learn>=0.5.3
